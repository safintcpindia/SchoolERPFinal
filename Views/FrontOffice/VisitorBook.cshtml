@model SchoolERP.Net.Models.FOVisitorBookPageViewModel
@using System.Security.Claims
@using SchoolERP.Net.Helpers
@inject PermissionHelper PermHelper
@{
    ViewData["Title"] = "Visitor Book";
    int currentUserId = 0;
    if (User.Identity != null && User.Identity.IsAuthenticated)
    {
        var idClaim = User.FindFirst(ClaimTypes.NameIdentifier) ?? User.FindFirst("UserId");
        if (idClaim != null) int.TryParse(idClaim.Value, out currentUserId);
    }
    if (currentUserId > 0) PermHelper.LoadPermissions(currentUserId);
    
    var canAdd = PermHelper.HasPermissionByUrl("/FrontOffice/VisitorBook", "Add");
    var canEdit = PermHelper.HasPermissionByUrl("/FrontOffice/VisitorBook", "Edit");
    var canDelete = PermHelper.HasPermissionByUrl("/FrontOffice/VisitorBook", "Delete");
    var canPrint = PermHelper.HasPermissionByUrl("/FrontOffice/VisitorBook", "Print");
    var canExport = PermHelper.HasPermissionByUrl("/FrontOffice/VisitorBook", "Export");

}
<style>
    .dt-buttons {
        display: none !important;
    }
</style>

<!-- Page Header -->
<div class="d-flex align-items-center justify-content-between flex-wrap gap-2 mb-2">
    <div>
        <h6 class="fw-bold mb-0">Visitor List</h6>
    </div>
    <div class="d-flex align-items-center flex-wrap gap-1">
        @if (canAdd)
        {
            <a href="javascript:void(0)" class="btn btn-primary" onclick="openAddModal()">
                <i class="ti ti-plus me-1"></i> Add
            </a>
        }
        @if (canEdit)
        {
            <button id="btnEdit" class="btn btn-info" disabled onclick="editSelected()">
                <i class="ti ti-pencil me-1"></i> Edit
            </button>
        }
        @if (canDelete)
        {
            <button id="btnDelete" class="btn btn-danger" disabled onclick="deleteSelected()">
                <i class="ti ti-trash me-1"></i> Delete
            </button>
        }
        @if (canPrint)
        {
            <button class="btn btn-secondary d-inline-flex align-items-center" onclick="triggerExport(4)">
                <i class="ti ti-printer me-1"></i>Print
            </button>
        }
        @if (canExport)
        {
            <div class="dropdown d-inline-flex">
                <button class="btn btn-warning dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <i class="ti ti-download me-1"></i>Export
                </button>
                <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="javascript:void(0)" onclick="triggerExport(0)"><i class="ti ti-copy me-1"></i>Copy</a></li>
                    <li><a class="dropdown-item" href="javascript:void(0)" onclick="triggerExport(1)"><i class="ti ti-file-text me-1"></i>CSV</a></li>
                    <li><a class="dropdown-item" href="javascript:void(0)" onclick="triggerExport(2)"><i class="ti ti-file-spreadsheet me-1"></i>Excel</a></li>
                    <li><a class="dropdown-item" href="javascript:void(0)" onclick="triggerExport(3)"><i class="ti ti-file-description me-1"></i>PDF</a></li>
                </ul>
            </div>
        }
    </div>
</div>

<div class="row">
    <!-- Filters -->
    <div class="col-md-12">
        <div class="card shadow-tbl-outer mb-2">
            <div class="card-body p-3">
                <div class="d-flex align-items-center justify-content-between flex-wrap gap-2">
                    <div class="position-relative border d-flex align-items-center rounded-2 px-3 ">
                        <i class="fas fa-search search-icon-input"></i>
                        <input type="text" class="form-control border-0 ps-2" id="txtSearchInput"
                               placeholder="Search by name, phone..." style="min-width: 220px;">
                    </div>
                    <div class="dropdown">
                        <a href="#" id="btnFilterToggle" class="btn btn-white border rounded btn-md text-dark fs-14 align-items-center d-flex fw-normal"
                           data-bs-toggle="dropdown" data-bs-auto-close="outside" aria-expanded="false">
                            <i class="ti ti-filter text-gray-5 me-1"></i>Filters
                        </a>
                        <div class="dropdown-menu dropdown-lg dropdown-menu-end filter-dropdown p-0 shadow-tbl" id="filter-dropdown">
                            <div class="d-flex align-items-center justify-content-between border-bottom filter-header">
                                <h6 class="mb-0 fw-bold">Select Criteria</h6>
                                <div class="d-flex align-items-center justify-content-end">
                                    <button type="button" class="btn text-primary fs-6 fw-medium" id="btnApplyFilters" onclick="applyFilters()">Filter</button>
                                </div>
                            </div>
                            <form action="#">
                                <div class="filter-body pb-0">
                                    <div class="row">
                                        <div class="col-md-12 mb-3">
                                            <label class="form-label mb-1">Purpose</label>
                                            <select class="form-select select2" id="filterPurpose">
                                                <option value="">All Purposes</option>
                                                @foreach (var p in Model.Purposes)
                                                {
                                                    <option value="@p.Name">@p.Name</option>
                                                }
                                            </select>
                                        </div>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>                       
                </div>
            </div>
        </div>
    </div>

    <!-- Data Table -->
    <div class="col-xl-12">
        <div class="card shadow-tbl">
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table mb-0" id="tblVisitors">
                        <thead>
                            <tr>
                                <th style="width: 50px;">Select</th>
                                <th>Purpose</th>
                                <th>Meeting With</th>
                                <th>Visitor Name</th>
                                <th>Phone</th>
                                <th>Date</th>
                                <th>In/Out Time</th>
                                <th></th>
                                <th>Status</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Visitors)
                            {
                                <tr class="item-row" onclick="selectItem(@item.VisitorBookID, this)" style="cursor: pointer;">
                                    <td class="ps-3">
                                        <input type="radio" name="selectedItem" class="form-check-input" value="@item.VisitorBookID">
                                    </td>
                                    <td>@item.PurposeName</td>
                                    <td>
                                        @if (item.MeetingWith == "Student") {
                                            <span class="text-primary">Student: @item.StudentName</span>
                                        } else if (item.MeetingWith == "Staff") {
                                            <span class="text-primary">Staff: @item.StaffName</span>
                                        } else {
                                            <span class="text-muted">-</span>
                                        }
                                    </td>
                                    <td>
                                        <div>@item.Name</div>
                                        <small>@item.IDCard</small>
                                    </td>
                                    <td>@item.Phone</td>
                                    <td>@item.Date.ToString("dd-MM-yyyy")</td>
                                    <td>
                                        <span class="">@item.InTime</span> /
                                        @if(!string.IsNullOrEmpty(item.OutTime)) {
                                            <span class="">@item.OutTime</span>
                                        }
                                    </td>
                                    <td>
                                        @if (!string.IsNullOrEmpty(item.FileName))
                                        {
                                            <a href="/FrontOffice/DownloadVisitorAttachment/@item.VisitorBookID" class="btn btn-sm btn-icon btn-outline-primary" title="Download Attachment">
                                                <i class="ti ti-download"></i>
                                            </a>
                                        }
                                    </td>
                                    <td>
                                        
                                        <div class="form-check form-switch d-inline-block ms-2" onclick="event.stopPropagation()">
                                            <input class="form-check-input" type="checkbox" 
                                                   onchange="toggleStatus(@item.VisitorBookID, this.checked)"
                                                   @(item.IsActive ? "checked" : "")
                                                   @(canEdit ? "" : "disabled")>
                                        </div>
                                    </td>
                                </tr>
                            }
                            @if (!Model.Visitors.Any())
                            {
                                <tr><td colspan="8" class="text-center text-muted py-4">No visitor records found.</td></tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Add / Edit Modal -->
<div class="modal fade" id="visitorModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered modal-lg">
        <div class="modal-content">
            <div class="modal-header d-flex justify-content-between align-items-center">
                <h5 class="modal-title" id="modalTitle">Add Visitor</h5>
                <div>
                    <button type="button" class="btn btn-success" onclick="saveVisitor()">
                        <i class="ti ti-cloud me-1"></i> Save
                    </button>
                    <button type="button" class="btn btn-danger" data-bs-dismiss="modal">
                        <i class="ti ti-x me-1"></i> Close
                    </button>
                </div>
            </div>
            <div class="modal-body">
                <form id="visitorForm" enctype="multipart/form-data">
                    <input type="hidden" id="hdnVisitorBookID" name="VisitorBookID" value="0">
                    <div class="row row-gap-3">
                        <div class="col-md-6">
                            <label class="form-label">Purpose <span class="text-danger">*</span></label>
                            <select class="form-select select2" id="ddlPurpose" name="PurposeID">
                                <option value="">Select</option>
                                @foreach(var p in Model.Purposes) { <option value="@p.PurposeID">@p.Name</option> }
                            </select>
                            <div id="errPurpose" class="invalid-feedback" style="display: none;">Purpose is required.</div>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Meeting With <span class="text-danger">*</span></label>
                            <select class="form-select select2" id="ddlMeetingWith" name="MeetingWith" onchange="handleMeetingWith(this.value)">
                                <option value="">Select</option>
                                <option value="Student">Student</option>
                                <option value="Staff">Staff</option>
                            </select>
                            <div id="errMeetingWith" class="invalid-feedback" style="display: none;">Meeting With is required.</div>
                        </div>

                        <!-- Student Selection Section -->
                        <div id="studentSelection" class="col-md-12 border rounded p-3 d-none bg-light">
                            <div class="row row-gap-3">
                                <div class="col-md-4">
                                    <label class="form-label">Class</label>
                                    <select class="form-select select2" id="ddlClass" onchange="loadSections(this.value)">
                                        <option value="">Select Class</option>
                                        @foreach(var c in Model.Classes) { <option value="@c.ClassID">@c.ClassName</option> }
                                    </select>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">Section</label>
                                    <select class="form-select select2" id="ddlSection" onchange="loadStudents()">
                                        <option value="">Select Section</option>
                                    </select>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">Student <span class="text-danger">*</span></label>
                                    <select class="form-select select2" id="ddlStudent" name="StudentID">
                                        <option value="">Select Student</option>
                                    </select>
                                    <div id="errStudent" class="invalid-feedback" style="display: none;">Student is required.</div>
                                </div>
                            </div>
                        </div>

                        <div id="staffSelection" class="col-md-12 border rounded p-3 d-none bg-light">
                            <label class="form-label">Staff Member <span class="text-danger">*</span></label>
                            <select class="form-select select2" id="ddlStaff" name="StaffID">
                                <option value="">Select Staff</option>
                                @foreach(var s in Model.Staff) { <option value="@s.StaffID">@s.FirstName @s.LastName (@s.StaffCode)</option> }
                            </select>
                            <div id="errStaff" class="invalid-feedback" style="display: none;">Staff Member is required.</div>
                        </div>

                        <div class="col-md-6">
                            <label class="form-label">Visitor Name <span class="text-danger">*</span></label>
                            <input type="text" class="form-control" id="txtName" name="Name" placeholder="Full Name">
                            <div id="errName" class="invalid-feedback" style="display: none;">Visitor Name is required.</div>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Phone</label>
                            <input type="text" class="form-control" id="txtPhone" name="Phone" placeholder="Contact Number">
                            <div id="errPhone" class="invalid-feedback"></div>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">ID Card</label>
                            <input type="text" class="form-control" id="txtIDCard" name="IDCard" placeholder="ID Card Detail">
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">No Of Persons</label>
                            <input type="number" class="form-control" id="txtNoOfPersons" name="NoOfPersons" value="1" min="1">
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Date <span class="text-danger">*</span></label>
                            <input type="date" class="form-control" id="txtDate" name="Date" value="@System.DateTime.Now.ToString("yyyy-MM-dd")">
                            <div id="errDate" class="invalid-feedback" style="display: none;">Date is required.</div>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">In Time</label>
                            <input type="text" class="form-control timepicker" id="txtInTime" name="InTime" placeholder="HH:MM AM/PM">
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Out Time</label>
                            <input type="text" class="form-control timepicker" id="txtOutTime" name="OutTime" placeholder="HH:MM AM/PM">
                        </div>
                        <div class="col-md-12">
                            <label class="form-label">Note</label>
                            <textarea class="form-control" id="txtNote" name="Note" rows="2" placeholder="Any additional information"></textarea>
                        </div>
                        <div class="col-md-12">
                            <label class="form-label">Attachment</label>
                            <input type="file" class="form-control" id="attachmentFile" name="attachmentFile">
                            <small class="text-muted" id="fileInfo"></small>
                        </div>
                        <div class="col-md-12">
                            <div class="form-check form-switch mt-2">
                                <input class="form-check-input" type="checkbox" id="chkActive" name="IsActive" checked value="true">
                                <label class="form-check-label" for="chkActive">Active</label>
                            </div>
                        </div>
                    </div>
                </form>
            </div>

        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/inline-form-validation.js"></script>
<script>
    let selectedId = 0;
    const canEdit = @canEdit.ToString().ToLower();
    const canDelete = @canDelete.ToString().ToLower();

        function triggerExport(index) {
            if ($.fn.DataTable.isDataTable('#tblVisitors')) {
                const table = $('#tblVisitors').DataTable();
                table.button(index).trigger();
            }
        }

        $(document).ready(function () {
            $('#tblVisitors').DataTable({
                dom: 'Bfrtip',
                buttons: [
                    { extend: 'copy', exportOptions: { columns: [1, 2, 3, 4] } },
                    { extend: 'csv', exportOptions: { columns: [1, 2, 3, 4] } },
                    { extend: 'excel', exportOptions: { columns: [1, 2, 3, 4] } },
                    { extend: 'pdf', exportOptions: { columns: [1, 2, 3, 4] } },
                    { extend: 'print', exportOptions: { columns: [1, 2, 3, 4] } }
                ],
                searching: false,
                paging: false,
                info: false
            });
        })



    $(document).ready(function() {
        // Initialize Select2 for page filters
        $('.select2:not(#visitorModal .select2)').select2({
            width: '100%'
        });

        // Initialize Select2 for modal with dropdownParent
        $('#visitorModal').on('shown.bs.modal', function () {
            $('#visitorModal .select2').select2({
                dropdownParent: $('#visitorModal'),
                width: '100%'
            });
        });

        // Clear validation errors on change/input
        const clearError = (id, errId) => {
            const el = document.getElementById(id);
            if (el && el.value.trim()) {
                el.classList.remove('is-invalid');
                document.getElementById(errId).style.display = 'none';
            }
        };

        $('#ddlPurpose, #ddlMeetingWith, #ddlStudent, #ddlStaff').on('change', function() {
            clearError(this.id, 'err' + this.id.replace('ddl', ''));
        });

        $('#txtDate').on('change', function() {
            clearError('txtDate', 'errDate');
        });

        $('#txtName').on('keyup', function() {
            clearError('txtName', 'errName');
        });
    });

    function handleMeetingWith(val) {
        document.getElementById('studentSelection').classList.toggle('d-none', val !== 'Student');
        document.getElementById('staffSelection').classList.toggle('d-none', val !== 'Staff');
        
        if (val === 'Student') {
            document.getElementById('ddlStudent').setAttribute('required', 'required');
            document.getElementById('ddlStaff').removeAttribute('required');
        } else if (val === 'Staff') {
            document.getElementById('ddlStaff').setAttribute('required', 'required');
            document.getElementById('ddlStudent').removeAttribute('required');
        } else {
            document.getElementById('ddlStudent').removeAttribute('required');
            document.getElementById('ddlStaff').removeAttribute('required');
        }
    }

    function loadSections(classId) {
        const ddl = $('#ddlSection');
        ddl.empty().append('<option value="">Select Section</option>');
        $('#ddlStudent').empty().append('<option value="">Select Student</option>');
        
        if (!classId) return;
        
        fetch(`/FrontOffice/GetSections?classId=${classId}`)
            .then(r => r.json())
            .then(res => {
                if (res.success) {
                    res.data.forEach(s => ddl.append(`<option value="${s.sectionID}">${s.sectionName}</option>`));
                }
            });
    }

    function loadStudents() {
        const classId = document.getElementById('ddlClass').value;
        const sectionId = document.getElementById('ddlSection').value;
        const ddl = $('#ddlStudent');
        ddl.empty().append('<option value="">Select Student</option>');
        
        if (!classId || !sectionId) return;
        
        fetch(`/FrontOffice/GetStudents?classId=${classId}&sectionId=${sectionId}`)
            .then(r => r.json())
            .then(res => {
                if (res.success) {
                    res.data.forEach(s => ddl.append(`<option value="${s.studentID}">${s.fullName} (${s.rollNo})</option>`));
                }
            });
    }

    function selectItem(id, row) {
        selectedId = id;
        document.querySelectorAll('.item-row').forEach(r => r.classList.remove('bg-light'));
        row.classList.add('bg-light');
        row.querySelector('input[type="radio"]').checked = true;

        if (canEdit) document.getElementById('btnEdit').disabled = false;
        if (canDelete) document.getElementById('btnDelete').disabled = false;
    }

    function applyFilters() {
        const q = document.getElementById('txtSearchInput').value.toLowerCase();
        const purpose = document.getElementById('filterPurpose').value;

        document.querySelectorAll('#tblVisitors tbody tr').forEach(row => {
            if (row.classList.contains('item-row')) {
                const text = row.textContent.toLowerCase();
                const rowPurpose = row.cells[1].textContent;

                const matchQ = !q || text.includes(q);
                const matchPurpose = !purpose || rowPurpose === purpose;

                row.style.display = (matchQ && matchPurpose) ? '' : 'none';
            }
        });
    }

    document.getElementById('txtSearchInput').addEventListener('keyup', applyFilters);
    document.getElementById('filterPurpose').addEventListener('change', applyFilters);

    function openAddModal() {
        document.getElementById('modalTitle').textContent = 'Add Visitor';
        document.getElementById('hdnVisitorBookID').value = 0;
        document.getElementById('visitorForm').reset();
        document.getElementById('txtDate').value = new Date().toISOString().split('T')[0];
        document.getElementById('chkActive').checked = true;
        document.getElementById('fileInfo').textContent = '';
        
        // Reset Meeting With
        $('#ddlMeetingWith').val('').trigger('change');
        handleMeetingWith('');

        $('#ddlPurpose, #ddlClass, #ddlSection, #ddlStudent, #ddlStaff').val('').trigger('change');
        
        new bootstrap.Modal(document.getElementById('visitorModal')).show();
    }

    async function editSelected() {
        if (!selectedId) return;
        const r = await fetch(`/FrontOffice/GetVisitor?id=${selectedId}`);
        const res = await r.json();
        if (!res.success) { showToast(res.message, 'danger'); return; }
        
        const d = res.data;
        document.getElementById('modalTitle').textContent = 'Edit Visitor';
        document.getElementById('hdnVisitorBookID').value = d.visitorBookID;
        document.getElementById('txtName').value = d.name;
        document.getElementById('txtPhone').value = d.phone || '';
        document.getElementById('txtIDCard').value = d.idCard || '';
        document.getElementById('txtNoOfPersons').value = d.noOfPersons;
        document.getElementById('txtDate').value = d.date.split('T')[0];
        document.getElementById('txtInTime').value = d.inTime || '';
        document.getElementById('txtOutTime').value = d.outTime || '';
        document.getElementById('txtNote').value = d.note || '';
        document.getElementById('chkActive').checked = d.isActive;
        document.getElementById('fileInfo').textContent = d.fileName ? 'Current file: ' + d.fileName : '';

        // Handle Meeting With
        const mw = (d.meetingWith === 'Student' || d.meetingWith === 'Staff') ? d.meetingWith : '';
        $('#ddlMeetingWith').val(mw).trigger('change');
        handleMeetingWith(mw);

        if (mw === 'Student' && d.studentID) {
            // We need to know class and section of the student to populate dropdowns.
            // For now, let's just fetch student details to get class/section.
            const sRes = await fetch(`/FrontOffice/GetStudentByID/${d.studentID}`);
            const sData = await sRes.json();
            if (sData.success) {
                const s = sData.data.basicInfo;
                $('#ddlClass').val(s.classID).trigger('change');
                
                // Wait for sections to load
                await new Promise(resolve => setTimeout(resolve, 300));
                $('#ddlSection').val(s.sectionID).trigger('change');
                
                // Wait for students to load
                await new Promise(resolve => setTimeout(resolve, 300));
                $('#ddlStudent').val(d.studentID).trigger('change');
            }
        } else if (mw === 'Staff' && d.staffID) {
            $('#ddlStaff').val(d.staffID).trigger('change');
        }

        $('#ddlPurpose').val(d.purposeID).trigger('change');

        new bootstrap.Modal(document.getElementById('visitorModal')).show();
    }

    function saveVisitor() {
        const form = document.getElementById('visitorForm');
        
        // Reset and validate Phone
        InlineFormValidation.clearFieldError('txtPhone', 'errPhone');
        const isPhoneValid = InlineFormValidation.validateMobile('txtPhone', 'errPhone', false);
        
        let isValid = true;
        const validateField = (id, errId) => {
            const el = document.getElementById(id);
            if (!el || !el.value.trim()) {
                if (el) el.classList.add('is-invalid');
                document.getElementById(errId).style.display = 'block';
                isValid = false;
            } else {
                el.classList.remove('is-invalid');
                document.getElementById(errId).style.display = 'none';
            }
        };

        validateField('ddlPurpose', 'errPurpose');
        validateField('ddlMeetingWith', 'errMeetingWith');
        validateField('txtName', 'errName');
        validateField('txtDate', 'errDate');

        const mw = document.getElementById('ddlMeetingWith').value;
        if (mw === 'Student') validateField('ddlStudent', 'errStudent');
        if (mw === 'Staff') validateField('ddlStaff', 'errStaff');

        if (!isValid || !isPhoneValid) { 
            return; 
        }

        const formData = new FormData(form);

        fetch('/FrontOffice/SaveVisitor', {
            method: 'POST',
            body: formData
        })
        .then(r => r.json())
        .then(res => {
            showToast(res.message, res.success ? 'success' : 'danger');
            if (res.success) {
                bootstrap.Modal.getInstance(document.getElementById('visitorModal')).hide();
                setTimeout(() => location.reload(), 800);
            }
        });
    }

    function deleteSelected() {
        if (!selectedId) return;
        if (!confirm('Are you sure you want to delete this visitor record?')) return;
        fetch(`/FrontOffice/DeleteVisitor?id=${selectedId}`, { method: 'POST' })
            .then(r => r.json())
            .then(res => {
                showToast(res.message, res.success ? 'success' : 'danger');
                if (res.success) setTimeout(() => location.reload(), 800);
            });
    }

    function toggleStatus(id, isActive) {
        fetch(`/FrontOffice/ToggleVisitorStatus?id=${id}&isActive=${isActive}`, { method: 'POST' })
            .then(r => r.json())
            .then(res => showToast(res.message, res.success ? 'success' : 'danger'));
    }

    function showToast(msg, type = 'success') {
        const wrapper = document.createElement('div');
        wrapper.innerHTML = `<div class="toast align-items-center text-bg-${type} border-0 show position-fixed bottom-0 end-0 m-3" role="alert" style="z-index:9999">
            <div class="d-flex"><div class="toast-body">${msg}</div>
            <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"></button>
            </div></div>`;
        document.body.appendChild(wrapper);
        setTimeout(() => wrapper.remove(), 3000);
    }
</script>
}
